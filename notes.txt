
V1: Query for underwriter data:
./run.sh query "Tabell med teckningsåtaganden och garantiåtaganden, med namn, belopp, andel % och totalsumma i SEK"

./run.sh query -k 1 "Tabell med teckningsåtaganden och garantiåtaganden, med namn, belopp, andel % och totalsumma i SEK" --save-image 


Next steps:

Feed in top 2 pages as images.

Make extracting modular, to easily run for different data types

Idea:
We have separate extract shell script.
./extract.sh underwriters
./extract.sh handelsstatistik
./extract.sh Villkor


Or...
./run.sh for everything

./run.sh index pathtopdf
./run.sh extract underwriters pathtopdf
./run.sh extract handell pathtopdf
./run.sh query pathtopfd "query text"